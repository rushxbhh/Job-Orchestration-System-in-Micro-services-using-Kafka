server:
  port: 8080

spring:
  application:
    name: job-service

  datasource:
    url: jdbc:mysql://localhost:3306/job_service_db
    username: root
    password: rushabh@2005
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    org.hibernate.engine.jdbc.spi.SqlExceptionHelper: ERROR

springdoc:
  swagger-ui:
    path: /swagger-ui.html
